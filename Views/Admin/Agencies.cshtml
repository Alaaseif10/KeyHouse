@{
    Layout = "~/Views/Shared/_LayoutAdmin.cshtml";
}
@* @model List<KeyHouse.Models.Entities.Users> *@
@model X.PagedList.IPagedList<KeyHouse.Models.Entities.Agencies>
@using X.PagedList.Mvc.Core;
<section>
    <div class="container-xxl py-5">
        <div class="container">
            <div class="text-center mx-auto mb-5 wow fadeInUp" data-wow-delay="0.1s" style="max-width: 600px;">
                <h1 class="mb-3">System Users</h1>

            </div>
            <div class="row g-4">
                @foreach (var item in Model)
                {
                    <div class="col-lg-3 col-md-6 wow fadeInUp" data-wow-delay="0.1s">
                        <div class="team-item rounded overflow-hidden">
                            <div class="position-relative">
                                <img class="img-fluid" src="~/img/@item.logo" alt="">
                                <div class="position-absolute start-50 top-100 translate-middle d-flex align-items-center">
                                </div>
                            </div>
                            <div class="text-center p-4 mt-3">
                                <h5 class="fw-bold mb-0">@item.Agency_Name</h5>
                                <label>Agency Contact Email:</label><small>@item.AgencyContactEmail</small>
                                <br />
                                <label>Agency Contact Phone:</label><small>@item.AgencyContactPhone</small>
                                <br />
                                <label>Status : </label><small> @(item.Agency_Status == 1 ? "Pending" : item.Agency_Status == 2 ? "Rejected" : "Approved")</small>
                            </div>
                        </div>
                    </div>
                }
            </div>
        </div>
    </div>
    @Html.PagedListPager(Model, page => Url.Action("Users", new { page }), new PagedListRenderOptions
{
    LiElementClasses = new string[] { "page-item" },
    PageClasses = new string[] { "page-link" }
})
